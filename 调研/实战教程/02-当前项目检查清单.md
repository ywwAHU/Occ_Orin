# 当前项目检查清单

## 这份清单怎么用

这不是讲原理的文档。

这是给你在推进项目时，逐项打勾用的。

建议每完成一项，就自己打一个勾。

## A. 当前主线确认

- 已确认当前 `V1` 主相机只用 `7` 路
- 已确认后向 `3` 路微光不作为当前主链路阻塞项
- 已确认 `5` 雷达保留，并先桥接为 `lidar_bev`
- 已确认 `INS` 必录
- 已确认毫米波建议录，但当前不是主模型刚需

## B. baseline 可运行确认

- 已在服务器上跑通 `smoke_test.py`
- 已跑通 synthetic `train.py`
- 已跑通 `export_onnx.py`
- 已确认当前配置文件是 `configs/baseline_orin_camera_lidar.json`

## C. 试采准备

- 已确定试采 trip 名称
- 已确定试采场景
- 已明确谁负责录制
- 已明确谁负责标定包整理
- 已明确谁负责采集元信息填写

## D. 试采数据完整性

- `7` 路主相机都有数据
- `5` 个雷达都有数据
- `INS` 有数据
- 每个传感器都有时间戳
- 有配套标定包
- 有 trip 元信息

## E. 标定与几何

- `7` 路相机内参齐全
- `7` 路相机畸变参数齐全
- `7` 路相机外参齐全
- `5` 个雷达外参齐全
- 已确认 `190` 度环视的图像处理方式
- 已确认训练图像尺寸和内参对应关系

## F. 原始 manifest

- 已生成第一版 `vehicle_raw_trial.jsonl`
- 相机命名与配置一致
- 雷达命名与脚本约定一致
- 图像路径真实存在
- 点云路径真实存在
- `ego_to_camera` 字段完整
- `lidar_to_ego` 字段完整

## G. 多雷达桥接

- 已运行 `build_vehicle_lidar_manifest.py`
- 已成功生成训练 manifest
- 已生成一批 `lidar_bev`
- `lidar_bev` 形状符合当前配置
- 没有出现大面积空 BEV

## H. 小样本闭环

- 已成功读取试采样本
- 已做单步前向或单步训练检查
- 已记录第一批问题清单

## I. 当前阶段不要做的事

- 不要一开始就录很多小时而不做链路验证
- 不要在没有标签方案前就大规模实车训练
- 不要把后三路微光相机先变成主阻塞项
- 不要忽视 `190` 度环视几何问题

## 一句话总结

如果这张清单的大部分都能打勾，说明你们已经真正从“概念讨论阶段”进入“项目落地阶段”了。
